# Start 使用说明

Start 是一款本地记事工具：把待办和念头拆成小步骤，帮你一次只做好一件事。数据仅存于设备本地，唯一的联网行为是检查更新。

> 本应用由个人开发者王浩然开发。开发者不编写程序代码，全程由 AI 编译。作者是锤子手机用户，深受 Smartisan OS 影响，交互逻辑参考其经典设计（闪念胶囊、大爆炸、一步），在此致敬。

## 使用心法

**丢 → 捋 → 做**

1. **丢**：想到什么全写下来，不必分类。
2. **捋**：抽空把暂存分拣到念头 / 日程 / 随手做。
3. **做**：一天专注一件事，事情大就拆成小步骤。

## 功能说明

### 念头

想法的速记入口。底部输入条直接书写，回车换行可多记几条，保存时按行自动拆分。

- 单击 → 拆词整理
- 双击 → 删除（6 秒内可撤销）
- 长按 → 批量选择
- 铅笔 → 编辑；设定时间后自动转为日程

### 动手吧

底栏中央红色功能键。将心中所想一股脑写下，点「倒进来」后按行与句末标点（。！？；…）自动拆成几件，进入捋一捋。

### 捋一捋

暂存内容的整理中枢，也支持「动手吧」的速记逻辑：底部输入条直接书写，回车换行多写几条，保存时按行与句末标点自动拆成多条暂存；点拆词按钮可先拆词，选中的词各成一条暂存。

- **分类**：逐条分到念头 / 日程（可选日期时间）/ 随手做，分类后自动流入首页对应区域
- **拆词**：按词拆开重组；词芯片默认全选，单击取消，双击删词，长按拖动换位
- **思维导图**：点按选中节点，可加子枝 / 加同级 / 编辑 / 删除，按住拖动连同子树移动，双指缩放
- **批量**：长按进入选择态，支持全选、批量分类与删除，均可撤销

### 首页

自上而下：大时钟 → 日程时间轴 → 随手做 → 今日焦点。日程右上角加号新建日程：打开后先选日期和时间，日期下方依次是提醒、写入日历、系统闹钟三项设置；写好内容并选好时间才会保存，没有内容或没选时间不会留下空白日程，也不会进入随手做。随手做右上角加号可一次写多件，回车换行或句末标点自动拆成多条，直接进随手做。

### 日程与时间轴

定时的任务按时刻排列在时间轴上，「现在」红线标示当前位置；已完成显示实心圆点，未到期为空心圆，过期仅标注日期，不变红、不责备。手机日历当天事件以小方点并排显示，与写入日历的任务自动去重。

**日程编辑三项设置**：

- **提醒**：到点弹出悬浮通知，设定时间后默认开启
- **写入日历**：存入手机日历，已写入的显示「已在日历」，点按直达
- **系统闹钟**：跳转系统时钟，由厂商闹钟响铃

### 随手做

未定时间的事项集中于此。右上角加号可一次写多件，回车换行或句末标点自动拆成多条直接存入；列表可拖动排序，进选择态可批量完成或删除。

### 今日焦点

页面底部浅红格子。将日程卡长按拖入即设为今日焦点，也可点圆钮从今日未完成中选定。

- 五个操作：进入专注、完成、拆成小步骤、编辑、更换一件
- 小步骤：输入条写一步存一步，回车换行可多写几步，按行自动拆分，内容也可拆词；标题仅为事件名，不参与拆分；全部完成后任务自动勾掉

### 专注

仅显示空心圆环与倒计时数字，无干扰。时长、提示音与音量在设置中调整。

### 统计

完成数量与专注时长，客观呈现进展。

## 提醒与后台

- 到点提醒由系统精准闹钟驱动，重启后自动重排
- 设置 → 后台保活：常驻通知 + 开机自启动，防止提醒被系统拦截；部分 ROM 需在系统设置中允许自启动、关闭省电限制

## 数据与更新

- 所有数据仅存设备本地，无账户、无云端
- 设置 → 数据：导出 / 导入 JSON；导入前自动快照，可撤销
- 有新版本时启动后自动提醒；设置 → 检查更新可手动查询

## 更新日志

- **v1.3.1**（versionCode 6）：底栏「开始」更名为「动手吧」；随手做（及念头、捋一捋新建）加号支持一次写多件，回车换行与句末标点自动拆成多条直接存入，全 app 拆分规则统一
- **v1.3**（versionCode 5）：新建日程交互优化——点日程加号后直接选日期时间，日期下方排列提醒、写入日历、系统闹钟三项设置；没有写内容或没选时间不保存，不会误建空白日程或流落随手做
- **v1.2**（versionCode 4）：新增开机动画启动页；捋一捋支持直接速记——底部输入条写下来自动拆成多条暂存，点拆词按钮可先拆词再存；开始页瘦身为纯速记，输入条与拆分规则全局统一
- **v1.1.1**（versionCode 111）：界面精简，「日程」小标题与右上角加号常驻
- **v1.1**（versionCode 11）：首页重排、时间轴「现在」红线、日程三开关（提醒 / 写入日历 / 系统闹钟）、日程卡拖拽设焦点
- **v1**（versionCode 1）：Flutter 从零重写首发

## 常见问题

**提醒不弹？** 允许 Start 的通知权限，保持后台保活开启；必要时在系统设置中允许自启动、关闭省电限制。

**数据会丢吗？** 仅存本机，卸载即删除。重要内容请用导出功能留存文件。

**能覆盖安装旧版吗？** 本版包名为 `com.astart.app`，与 Java 架构版（`com.start.notes`）包名不同，无法覆盖，需卸载后安装；数据可用旧版导出的 JSON 恢复。

## 协议摘要

- 对个人非商业使用**永久免费**，商业使用需开发者书面授权
- 完整用户协议与隐私政策见应用内设置；开源许可证为 GPLv3，全文见 [LICENSE](LICENSE)（项目源码：https://gitee.com/dubwhr/astart ）

── 工匠的骄傲与喜悦 · PRIDE & JOY ──
